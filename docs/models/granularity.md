# Granularity

The granularity of the report.

## Example Usage

```python
from censys_platform.models import Granularity

value = Granularity.DAILY

# Open enum: unrecognized values are captured as UnrecognizedStr
```


## Values

| Name      | Value     |
| --------- | --------- |
| `DAILY`   | daily     |
| `MONTHLY` | monthly   |