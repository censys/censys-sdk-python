"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from censys_platform.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class RippleClioResultsTypedDict(TypedDict):
    clio_version: NotRequired[str]
    rippled_version: NotRequired[str]
    validated: NotRequired[bool]
    validation_quorum: NotRequired[int]


class RippleClioResults(BaseModel):
    clio_version: Optional[str] = None

    rippled_version: Optional[str] = None

    validated: Optional[bool] = None

    validation_quorum: Optional[int] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["clio_version", "rippled_version", "validated", "validation_quorum"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
