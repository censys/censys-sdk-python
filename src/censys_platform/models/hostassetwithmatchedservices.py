"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .host import Host, HostTypedDict
from .matchedservice import MatchedService, MatchedServiceTypedDict
from censys_platform.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Any, Dict, List
from typing_extensions import NotRequired, TypedDict


class HostAssetWithMatchedServicesTypedDict(TypedDict):
    extensions: Dict[str, Any]
    resource: HostTypedDict
    matched_services: NotRequired[Nullable[List[MatchedServiceTypedDict]]]
    r"""The host services that match the query."""


class HostAssetWithMatchedServices(BaseModel):
    extensions: Dict[str, Any]

    resource: Host

    matched_services: OptionalNullable[List[MatchedService]] = UNSET
    r"""The host services that match the query."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["matched_services"])
        nullable_fields = set(["matched_services"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
