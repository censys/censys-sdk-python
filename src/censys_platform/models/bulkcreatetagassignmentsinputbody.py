"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from censys_platform.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class BulkCreateTagAssignmentsInputBodyTypedDict(TypedDict):
    query: str
    r"""A CenQL query identifying the assets to tag."""
    max_assets: NotRequired[int]
    r"""Optional upper bound on the number of assets to tag in this operation. This is your own limit for this request, separate from your plan's tag asset limit: the effective cap is the smaller of this value, your plan's tag asset limit, and the number of matching results. Omit or set to 0 for no explicit cap."""


class BulkCreateTagAssignmentsInputBody(BaseModel):
    query: str
    r"""A CenQL query identifying the assets to tag."""

    max_assets: Optional[int] = None
    r"""Optional upper bound on the number of assets to tag in this operation. This is your own limit for this request, separate from your plan's tag asset limit: the effective cap is the smaller of this value, your plan's tag asset limit, and the number of matching results. Omit or set to 0 for no explicit cap."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["max_assets"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
