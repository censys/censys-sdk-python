"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from censys_platform.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class RsaPublicKeyTypedDict(TypedDict):
    exponent: NotRequired[int]
    r"""The RSA key's public exponent (e)."""
    length: NotRequired[int]
    r"""Bit-length of the RSA modulus."""
    modulus: NotRequired[str]
    r"""The RSA key's modulus (n) in big-endian encoding."""


class RsaPublicKey(BaseModel):
    exponent: Optional[int] = None
    r"""The RSA key's public exponent (e)."""

    length: Optional[int] = None
    r"""Bit-length of the RSA modulus."""

    modulus: Optional[str] = None
    r"""The RSA key's modulus (n) in big-endian encoding."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["exponent", "length", "modulus"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
