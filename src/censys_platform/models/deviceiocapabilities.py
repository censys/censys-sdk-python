"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from censys_platform.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class DeviceIOCapabilitiesTypedDict(TypedDict):
    audio_outputs: NotRequired[int]
    audio_sources: NotRequired[int]
    digital_inputs: NotRequired[int]
    relay_outputs: NotRequired[int]
    serial_ports: NotRequired[int]
    video_outputs: NotRequired[int]
    video_source: NotRequired[int]


class DeviceIOCapabilities(BaseModel):
    audio_outputs: Optional[int] = None

    audio_sources: Optional[int] = None

    digital_inputs: Optional[int] = None

    relay_outputs: Optional[int] = None

    serial_ports: Optional[int] = None

    video_outputs: Optional[int] = None

    video_source: Optional[int] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "audio_outputs",
                "audio_sources",
                "digital_inputs",
                "relay_outputs",
                "serial_ports",
                "video_outputs",
                "video_source",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
