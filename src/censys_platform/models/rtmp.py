"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .rtmp_connectstatus import RtmpConnectStatus, RtmpConnectStatusTypedDict
from censys_platform.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class RtmpTypedDict(TypedDict):
    capabilities: NotRequired[int]
    mode: NotRequired[int]
    peer_bandwidth: NotRequired[int]
    protocol_version: NotRequired[int]
    server_software: NotRequired[str]
    status: NotRequired[RtmpConnectStatusTypedDict]
    window_acknowledgement_size: NotRequired[int]


class Rtmp(BaseModel):
    capabilities: Optional[int] = None

    mode: Optional[int] = None

    peer_bandwidth: Optional[int] = None

    protocol_version: Optional[int] = None

    server_software: Optional[str] = None

    status: Optional[RtmpConnectStatus] = None

    window_acknowledgement_size: Optional[int] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "capabilities",
                "mode",
                "peer_bandwidth",
                "protocol_version",
                "server_software",
                "status",
                "window_acknowledgement_size",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
