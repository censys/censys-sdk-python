"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from censys_platform import models, utils
from censys_platform.types import BaseModel, UNSET_SENTINEL
from datetime import datetime
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class TagPrivacy(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members."""

    PRIVATE = "private"
    SHARED = "shared"


class TagTypedDict(TypedDict):
    created_at: datetime
    r"""RFC3339 timestamp when the tag was created."""
    created_by: str
    r"""The ID of the user or service account that created the tag."""
    id: str
    r"""The unique ID of the tag."""
    name: str
    r"""The display name of the tag."""
    privacy: TagPrivacy
    r"""Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members."""
    updated_at: datetime
    r"""RFC3339 timestamp when the tag was last updated."""
    description: NotRequired[str]
    r"""A human-readable description of the tag."""


class Tag(BaseModel):
    created_at: datetime
    r"""RFC3339 timestamp when the tag was created."""

    created_by: str
    r"""The ID of the user or service account that created the tag."""

    id: str
    r"""The unique ID of the tag."""

    name: str
    r"""The display name of the tag."""

    privacy: TagPrivacy
    r"""Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members."""

    updated_at: datetime
    r"""RFC3339 timestamp when the tag was last updated."""

    description: Optional[str] = None
    r"""A human-readable description of the tag."""

    @field_serializer("privacy")
    def serialize_privacy(self, value):
        if isinstance(value, str):
            try:
                return models.TagPrivacy(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["description"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
