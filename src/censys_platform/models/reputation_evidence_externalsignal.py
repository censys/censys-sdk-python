"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from censys_platform import models, utils
from censys_platform.types import BaseModel, UNSET_SENTINEL
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class Tlp(str, Enum, metaclass=utils.OpenEnumMeta):
    UNKNOWN = ""
    GREEN = "green"
    AMBER = "amber"
    RED = "red"
    WHITE = "white"


class ReputationEvidenceExternalSignalTypedDict(TypedDict):
    description: NotRequired[str]
    source: NotRequired[str]
    tlp: NotRequired[Tlp]


class ReputationEvidenceExternalSignal(BaseModel):
    description: Optional[str] = None

    source: Optional[str] = None

    tlp: Optional[Tlp] = None

    @field_serializer("tlp")
    def serialize_tlp(self, value):
        if isinstance(value, str):
            try:
                return models.Tlp(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["description", "source", "tlp"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
