"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from censys_platform.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, Optional
from typing_extensions import NotRequired, TypedDict


class RealportTypedDict(TypedDict):
    hardware_id: NotRequired[int]
    hardware_version: NotRequired[int]
    num_ports: NotRequired[int]
    product_name: NotRequired[str]
    software_version: NotRequired[int]
    unpatched_etherlite: NotRequired[bool]
    vpd: NotRequired[Dict[str, str]]


class Realport(BaseModel):
    hardware_id: Optional[int] = None

    hardware_version: Optional[int] = None

    num_ports: Optional[int] = None

    product_name: Optional[str] = None

    software_version: Optional[int] = None

    unpatched_etherlite: Optional[bool] = None

    vpd: Optional[Dict[str, str]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "hardware_id",
                "hardware_version",
                "num_ports",
                "product_name",
                "software_version",
                "unpatched_etherlite",
                "vpd",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
