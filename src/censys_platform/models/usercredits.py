"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from censys_platform.types import BaseModel, UNSET_SENTINEL
from datetime import datetime
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class UserCreditsTypedDict(TypedDict):
    balance: int
    r"""The current credit balance for the user."""
    resets_at: NotRequired[datetime]
    r"""The date that the user's credits will be reset."""


class UserCredits(BaseModel):
    balance: int
    r"""The current credit balance for the user."""

    resets_at: Optional[datetime] = None
    r"""The date that the user's credits will be reset."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["resets_at"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
