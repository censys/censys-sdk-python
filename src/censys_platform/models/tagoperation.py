"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from censys_platform import models, utils
from censys_platform.types import BaseModel, UNSET_SENTINEL
from datetime import datetime
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class TagOperationStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The current status of the operation."""

    PENDING = "pending"
    RUNNING = "running"
    SUCCEEDED = "succeeded"
    LIMIT_REACHED = "limit_reached"
    FAILED = "failed"
    CANCELLED = "cancelled"


class TagOperationType(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""Whether the operation creates or deletes tag assignments."""

    BULK_CREATE = "bulk_create"
    BULK_DELETE = "bulk_delete"


class TagOperationTypedDict(TypedDict):
    created_at: datetime
    r"""RFC3339 timestamp when the operation was created."""
    id: str
    r"""The unique ID of the operation."""
    processed_count: int
    r"""The number of assets that have been processed so far."""
    status: TagOperationStatus
    r"""The current status of the operation."""
    successful_count: int
    r"""The number of assets that have been successfully processed so far."""
    tag_id: str
    r"""The ID of the tag the operation applies to."""
    tag_name: str
    r"""Snapshot of the tag's display name at the time the operation was created."""
    total_count: int
    r"""The total number of assets the operation will process. For bulk create operations this is an approximate value set at the start; for bulk delete operations it is set at completion."""
    type: TagOperationType
    r"""Whether the operation creates or deletes tag assignments."""
    ended_at: NotRequired[datetime]
    r"""RFC3339 timestamp when the operation completed. Omitted while the operation is still in progress."""
    error_message: NotRequired[str]
    r"""Failure detail. Only set when the operation failed."""
    query: NotRequired[str]
    r"""The CenQL query used to identify the assets to tag. Only set for bulk create operations."""
    status_message: NotRequired[str]
    r"""A human-readable explanation of the operation's outcome, set once the operation finishes — for example, how many assets were assigned, or that the tag's plan asset limit was reached. For failed operations it mirrors error_message."""


class TagOperation(BaseModel):
    created_at: datetime
    r"""RFC3339 timestamp when the operation was created."""

    id: str
    r"""The unique ID of the operation."""

    processed_count: int
    r"""The number of assets that have been processed so far."""

    status: TagOperationStatus
    r"""The current status of the operation."""

    successful_count: int
    r"""The number of assets that have been successfully processed so far."""

    tag_id: str
    r"""The ID of the tag the operation applies to."""

    tag_name: str
    r"""Snapshot of the tag's display name at the time the operation was created."""

    total_count: int
    r"""The total number of assets the operation will process. For bulk create operations this is an approximate value set at the start; for bulk delete operations it is set at completion."""

    type: TagOperationType
    r"""Whether the operation creates or deletes tag assignments."""

    ended_at: Optional[datetime] = None
    r"""RFC3339 timestamp when the operation completed. Omitted while the operation is still in progress."""

    error_message: Optional[str] = None
    r"""Failure detail. Only set when the operation failed."""

    query: Optional[str] = None
    r"""The CenQL query used to identify the assets to tag. Only set for bulk create operations."""

    status_message: Optional[str] = None
    r"""A human-readable explanation of the operation's outcome, set once the operation finishes — for example, how many assets were assigned, or that the tag's plan asset limit was reached. For failed operations it mirrors error_message."""

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return models.TagOperationStatus(value)
            except ValueError:
                return value
        return value

    @field_serializer("type")
    def serialize_type(self, value):
        if isinstance(value, str):
            try:
                return models.TagOperationType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["ended_at", "error_message", "query", "status_message"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
