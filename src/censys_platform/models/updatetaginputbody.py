"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from censys_platform.types import BaseModel, UNSET_SENTINEL
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class UpdateTagInputBodyPrivacy(str, Enum):
    r"""Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members."""

    PRIVATE = "private"
    SHARED = "shared"


class UpdateTagInputBodyTypedDict(TypedDict):
    description: NotRequired[str]
    r"""New description."""
    name: NotRequired[str]
    r"""New display name. Must be unique within the organization."""
    privacy: NotRequired[UpdateTagInputBodyPrivacy]
    r"""Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members."""


class UpdateTagInputBody(BaseModel):
    description: Optional[str] = None
    r"""New description."""

    name: Optional[str] = None
    r"""New display name. Must be unique within the organization."""

    privacy: Optional[UpdateTagInputBodyPrivacy] = None
    r"""Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["description", "name", "privacy"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
